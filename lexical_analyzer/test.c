#include <stdio.h>

/* COMMENT


 */
char wt_Chk      (float wt2, float m_in, float max){
	if (wt2 > max || wt2 < min) return 'T';
	else return 'F';
}

/* COMMENT */
int main() {

	if (x==0){
		x++;
	}
	float abc#[]=100, min=0maximum;
	/* COMMENT */
	int arR[6]={1,0,2,3,3,3};
	, Maz;
	"THESE violent delights have violent ends!"
	printf("wt Is in range: %c", WT_CHK(67, min, max));
}
			